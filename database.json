const { MongoClient } = require("mongodb");

const client = new MongoClient(process.env.MONGO_URI);
let db;

async function connectMongo() {
  await client.connect();
  db = client.db("api-discordia");
  console.log("Mongo conectado");
}

connectMongo();